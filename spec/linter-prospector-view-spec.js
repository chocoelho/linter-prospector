'use babel';

import LinterProspectorView from '../lib/linter-prospector-view';

describe('LinterProspectorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
